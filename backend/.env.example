# 数据库类型：sqlite（本地测试）或 mariadb（生产环境）
DB_TYPE=sqlite

# MariaDB配置（仅DB_TYPE=mariadb时需要）
DB_HOST=localhost
DB_USER=edu_user
DB_PASSWORD=your_password_here
DB_NAME=edu_system

# JWT密钥
JWT_SECRET=your-secret-key-here

# 服务器端口
PORT=3001
